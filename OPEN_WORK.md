# envlib ingestion — Open Work

The **datasets/ingestion track** of the envlib project: this toolkit, the per-source ingest
repos (`envlib-ingest-*`), dataset selection/licensing, and the tethys→envlib data migration.
Stack *development* (envlib/cfdb/ebooklet package work) is tracked separately in
`envlib-repos/envlib/OPEN_WORK.md`. Same conventions: **verification debts** (work done but
not yet proven; remove once the close-check passes) + **backlog**; completed items move to
**Done** at the bottom. Sessions always launch from the envlib hub repo regardless of track.

## Verification debts

*(none currently.)*

## Backlog

- [ ] **[toolkit] `merge_dataset` should clamp its own block width — today a consumer guard is the only protection** (found 2026-08-06, dual-blind reviewed in round `chunked-1`; both arms flagged it independently). `merge_dataset` derives `w_lo` from the **global minimum incoming timestamp** across all stations (`tsortho.py:685`) and then reads `dv[:, w_lo:w_hi+1]` across **every point** (`:706`), holding ~6–7 float64 blocks plus masks. **One backdated row from one station widens the block for all of them.** Measured by a review arm on a 100 × 61,000 dataset: a normal 48-step merge peaks at **9 MB**; the same window plus a *single* backdated observation widens the block to 60,900 steps and peaks at **228 MB** — a 25× jump. At streamflow scale (261 × 565k) that extrapolates to **~8 GB inside an hourly cron container**, plus a rewrite of essentially every chunk and the matching upload churn. Not corruption (the union-mask merge is idempotent), but an availability failure.
  **Mitigated at the consumer for now, NOT in the toolkit:** `envlib-ingest-ecan-env/raw/ingest.py` now applies `_assert_within_horizon` to *both* the update and the heal paths (the heal path previously skipped it deliberately — that was the live hole). But a guard living in the caller is backwards: the next consumer inherits none of it, and the toolkit's own docstring claims "no full-remote materialization is required", which is true of the *pull* and misleading about the *block*.
  **Fix — ITERATE BY STATION, don't clamp the width** (revised 2026-08-06 on Mike's design ruling: *everything should iterate by station; a per-station chunk is never too large to hold in RAM*). Clamping was the first idea and it is the worse one — it caps the damage while keeping the all-points read, and it forces an awkward "refuse vs silently narrow" choice.
  **The merge has NO cross-station dependency**, so per-station is simply correct: the union mask (`tsortho.py:751-753`) is elementwise, and `incoming[row, col]` only ever carries data for the station at `row`. So each station can be read, merged and written on its own:

  ```python
  for ref, (t, v, ex) in non_empty.items():
      row = id_to_row[sid]
      col = _step_index(t, t0, step_us)
      lo, hi = int(col.min()), int(col.max())      # THIS station's window, not the global one
      existing_row = np.asarray(dv[row, lo:hi + 1].data, dtype='float64')
      ...  # scatter, union mask, np.where — all unchanged, just one row wide
      dv[row, lo:hi + 1] = _nan_safe(merged_row, dv.dtype)
  ```

  Three wins over clamping:
  1. **Memory is bounded by one station's window**, not `n_stations × global_window`. Even a pathological full-axis window for a single station is ~5 MB.
  2. **A backdated timestamp widens only ITS OWN station's read.** The hazard essentially evaporates rather than being capped — no policy decision needed about what bound to enforce.
  3. **It is chunk-aligned.** `chunk_shape` is `(1, time_chunk)`, so `dv[:, lo:hi+1]` already touches at least one chunk *per station*; the current code just holds them all at once. Per-station reads the same chunks and holds one.

  Care needed on: the `n_before` (pre-axis values dropped) accounting and the `gap_steps` return, which are currently computed over the whole block; and the axis extension (`new_steps`), which is separate and stays as-is. Needs its own round — it is a behaviour change on a live path — but it is now a *simplification* rather than a policy addition.
  **Note the consumer-side guard is still worth keeping** (`ecan-env/raw/ingest.py:_assert_within_horizon`): it stops genuinely bad data reaching the store at all, which is a different job from bounding memory.

- [ ] **[toolkit] The `series` contract still materialises every station before the build starts — make it an iterator** (the second half of the chunked-write fix; the first half is DONE, see below). `build_local(..., series: dict)` and `fetch_all(...) -> {ref: (times, values, extras)}` mean the caller holds every station's arrays at once: ~24 B per stored observation, measured **784 MB for ECan groundwater**. That is now the *entire* remaining floor of the build — the planes are gone. Fixing it needs both halves: `build_local` accepting an iterable of `(ref, times, values, extras)`, and the source adapters *yielding* per station (`envlib-ingest-ecan-env/qa/source.py`, `raw/source.py`).
  **The real design work is the two-pass problem:** the time-axis extent must be known before any row can be written (`build_local` currently reduces `min`/`max` over the whole dict), so either the caller declares the span, or the source must be re-iterable rather than a one-shot generator, or a cheap metadata pre-pass collects per-station min/max. Decide explicitly.
  Breaking change ⇒ **0.2.x → 0.3.0**. Blast radius is small: `envlib-ingest-ecan-env` is the only consumer (`envlib-ingest-esa-sst` has its own grid publish path). Per-station validation (`_check_aligned`, `_check_extras`, `_qc_filter`, `_non_empty`) is already single-pass with no cross-station state, so it folds into the streaming loop; `_check_refs` needs only the key set.

- [ ] **[tethys] BLOCKER for every tethys migration: `tethysts` and this toolkit cannot share an environment** (found 2026-08-03, building the ECan QC migration). `tethysts` → `pandas<2` → the numpy 1.x C ABI; `envlib-ingest-base>=0.2.0` → `cfdb>=0.9.4` → `numpy>2`, and that cfdb floor is asserted at RUNTIME (older cfdb fabricates values through packed dtypes, so it cannot be relaxed). `uv` reports the pair as flatly unsatisfiable — there is no pinning that reconciles them, and splitting locks does not help because neither side is individually satisfiable *together with* the other in one process. It surfaces in stages, so expect confusion: pandas 1.5.3 has no cp312 wheel; capping Python at 3.11 gets it installed; it then dies at import with `numpy.dtype size changed ... Expected 96 from C header, got 88`; only pinning `numpy<2` makes uv state the real conflict. **Resolution adopted: a standalone tethys→netCDF4 archiver** (own repo, own venv, quarantined legacy pins, retired when the migration completes) that faithfully exports each tethys dataset with ALL its metadata; the `envlib-ingest-*` repos then read that export with xarray in the modern environment and never see tethysts. This also backs up single-copy archives and covers datasets envlib cannot represent (e.g. 30 of ECan's 36 QC'd datasets are irregular). Getting its own plan. **Verified escape hatch if a direct reader is ever wanted instead:** a tethys result chunk decodes completely with `zstandard` + `xarray(engine='scipy')` on numpy 2.3.2 — note the chunks are netCDF-3 classic, so `h5netcdf` fails and the scipy engine is required.
- [ ] **[toolkit] No deletion path — the merge cannot express a retraction** (found during the 2026-08-03 ancillary-variable round; Gemini flagged it, diagnosis corrected on inspection): `merge_dataset` writes "incoming non-NaN wins", so an all-NaN incoming slot leaves the stored value untouched. That is **deliberate and load-bearing** — it is exactly what stops a briefly-offline station from erasing good history (`tsortho.py` module docstring) — but it means a source that legitimately revises a reading *to* missing has no way to say so, and the toolkit has zero delete/retract functions. Harmless for frozen archives (`build_local` only, merge never exercised) and for the current raw feeds (absence arrives as absent stamps, not as explicit NaN). **Needed before any LIVE quality-controlled feed**, where an upstream retraction must propagate. Design note: the fix is an explicit deletion mechanism (a sentinel, or an explicit per-slot mask argument), NOT relaxing the merge rule — relaxing it would break the offline-station protection. Gemini's framing of this as a "merge logic flaw" is wrong and should not be adopted verbatim if the item is picked up.
- [ ] **[toolkit] Consider promoting the ingest orchestration into envlib-ingest-base — DEFERRED until ≥2–3 sources exist** (Mike, 2026-07-22): after the template genericization, `envlib-ingest-ecan-env`'s `ingest.py`+`deregister.py` are source-agnostic ENGINE (only the `source` adapter + `config` are per-source). Tempting to extract that engine into the toolkit (e.g. a `run_ingest(config, source, commons, member)` entrypoint) so a bug fix lands ONCE instead of per-repo — but deliberately held: the adapter interface (`discover_stations`/`fetch_and_resample`) + the backfill/update/deep-sweep/heal loop are proven against ONE source (ECan) only. Add 2–3 more ts_ortho sources via the copy-the-template flow first; if the engine stays stable AND the copy-duplication actually bites, extract it then. Extracting now risks freezing ECan-shaped assumptions as the "generic" engine. Trigger: the 2nd/3rd source's needs (non-CSV fetch, auth, a different cadence, a non-station shape) reveal what is truly generic. (Same evidence-first stance as the creds-helper "promote when a 2nd repo needs it" note.)
- [ ] **[toolkit] `statistic='point'` engine for the resampler** (Mike, 2026-07-20): pass-through of instantaneous samples for native-cadence datasets published as-is — the envlib `aggregation_statistic` CV already has the `'point'` code, and `resample()` currently raises cleanly on it. Motivation: scripts should iterate datasets passing `statistic=metadata['aggregation_statistic']` blindly (mean/sum/point alike) with the resampler owning the dispatch. Design settled in discussion: NO estimation of any kind — after `_prep` (round_to + dedupe), keep only readings exactly on the epoch-anchored freq grid, values untouched; off-grid readings are absent (a point dataset never interpolates to an instant it didn't observe); producers wanting full snapping declare `round_to=freq` explicitly; `max_gap`/`gap_multiplier`/`min_coverage` accepted for API symmetry but unused (no intervals). Note the mean-at-native-freq distinction that motivated this: `statistic='mean'` at source-freq==output-freq correctly yields interval means (endpoint averages, n−1 points) per its declaration — `'point'` is the semantic for identity pass-through. Tiny engine + fixtures + README statistic entry + toolkit version bump; build when the first point-semantics dataset arrives.
- [ ] **[ingest] Phase-anchored datasets (local-midnight / 9am rain-day boundaries)** (designed follow-up from the 2026-07-17 tsortho-generalization review; Gemini argued phase-now, Claude argued the guard-first path — adopted): today tsortho enforces strict epoch anchoring in BOTH directions (data alignment guard + metadata phase guard: fixed cadence with reduced `utc_offset`≠`+00:00` rejected). Supporting phase needs ONE designed feature: resampler `origin` parameter (bin boundaries at epoch+phase) + phase-aware tsortho guard/axis (phase from the hashed `utc_offset`, NOT the unhashed axis t0) + natural-unit rule becomes `gcd(step, phase)` (a `+12:00` daily axis needs `[h]`, not `[D]`) — and note the 9am NZ rain day may not be expressible via `utc_offset` at all (±14h/quarter-hour clamp), so the origin feature is the primary path. Pick up before any local-day daily product (e.g. the parked NZ precip candidate).
- [ ] **[tethys] Build the tethys → envlib migration pipeline** (added 2026-07-08; the next major project phase after public-RCG hosting): walk the tethys production datasets into envlib form — apply the plan's migration notes (feature/variable renames, frequency + utc_offset conversion, product_code decomposition, interval-start timestamps), re-derive ids via `Metadata`, build cfdb files, publish into the commons catalogue. Scope/tooling (where the pipeline lives, per-repo vs central) to be planned when picked up; `docs/guide/tethys-migration.md` + the plan's migration-notes section are the specs.
- [ ] **[envlib] First commons datasets — static-first, fresh-first (Mike's direction, 2026-07-14; dataset choice OPEN)**: fill the commons starting with STATIC datasets (not continuously updated — no update pipeline needed, defers the quasi-static/back-fill machinery), and preferably first a dataset NOT already in tethys — so the first real publish exercises envlib's native producer flow (metadata authoring from scratch, CV coverage, license/attribution) rather than the tethys migration adapter, and the first commons entry is new value rather than duplicated data. Candidate OPEN again (2026-07-14: Mike parked the precip product as the FIRST dataset — the NIWA licensing complication makes it a poor opener; 'I don't want a complicated first dataset'; he is choosing another). One-question filter for candidates, learned from the precip round: can the license/provenance file be produced TODAY? PARKED LATER CANDIDATE — the `~/git/monitoring-data-processing` NZ precipitation product (ts_ortho; hourly + daily; climate-combined provenance level; ~4.2k stations, records to 1852; frozen June-2026 snapshot). Ownership ruling direction: owner = the curator (GHCN model — the merge/QC/combine are editorial acts), sources honored via CC-BY attribution. **Licensing gate**: the NIWA-sourced portion (~46% of station-rows) has licence restrictions incompatible with open republication (analysis + resolution paths in project memory: `project_niwa_licensing`); the publishable path is the CC-BY-only variant (~3.0k of 5.6k station-rows, councils+FENZ+MetService; requires a pipeline re-run with NIWA sources excluded). Ingest-template requirement adopted from this round: a per-source PROVENANCE RECORD in every ingest repo (license text or written confirmation + acquisition date + channel); for councils, the CC-BY notice usually lives on their data-access pages — capture it at acquisition time. The tethys migration follows once the native flow has shaken out.

## Done (newest first)

- [x] ~~**[toolkit] `build_local` writes ROW BY ROW instead of materialising the dense plane**~~ — **DONE 2026-08-06**, dual-blind reviewed at plan stage (round `chunked-1`, Fable 5 high + Gemini 3.1 Pro high). `_assemble` built `np.full((len(stations), n_times))` per plane and handed it to a single `dv[:] = _nan_safe(plane)`. **That defeated the main reason to use cfdb** — it reads and writes in chunks precisely so a dataset need never be resident, and the ts_ortho `chunk_shape` was already `(1, 25_000)`, *one station row*, so the natural write unit was a single station the code declined to use. Cost scaled with the **dimensions**, not the data: groundwater is 8.7 % dense yet paid 3.01 GB per plane, twice, plus `_nan_safe`'s mask and copy.
  Replaced by `_write_rows`, which builds one row and writes `var[i, :]` per station, for the primary and every ancillary. **Measured (400 stations × 150,000 steps, 0.8 % dense):** traced peak **1,506.5 MB → 10.9 MB** (138x), RSS 1,625 MB → 162 MB, and **faster** — 2.28 s → 1.49 s, since the giant transient allocations are gone.
  **Output is unchanged, verified at the stored-key level**: 4,813/4,813 booklet keys byte-identical with identical file size, on a fixture carrying an ancillary plane *and* data-less stations. Decoded values, NaN masks, attrs, coords and station vars all equal. (A file checksum is the wrong gate — two independent builds differ by cfdb's per-file uuid.)
  **Three constraints the round established, all now enforced or tested:** iterate `stations`, never `series` (driving from `series` maps the *i*-th station-with-data to point *i* and silently mislabels every station after the first gap — `test_rows_align_to_stations_not_to_series`); write each chunk exactly once (a revisit costs decompress+recompress, orphans the old block in a log-structured store, and can force a sync — `test_each_chunk_written_exactly_once`); and keep the coordinate write **bulk**, since `Coordinate.append` is O(entire coordinate) per call (measured by a review arm at 927x slower in a loop).
  103 tests pass (100 pre-existing unmodified + 3 new). `test_build_local_peak_allocation_is_bounded_by_one_row` was mutation-tested: against the pre-refactor code it fails with *"peak 603.8 MB exceeds 20 rows (24.0 MB)"*. **Root cause of the habit, also fixed:** the `cfdb` skill and `cfdb_summary_usage.md` both led their "Writing Data" section with `temp[:] = data_array` labelled *"write all data at once"*; both now lead with the incremental form, `CLAUDE.md`/`GEMINI.md` carry the principle, and a `PostToolUse` hook flags whole-array patterns in files touching cfdb/netCDF/xarray.

- [x] ~~**[envlib+ingest] ECan surface-water + precip ingest — BUILT, backfilled, and LIVE on the swarm cron**~~ — **DONE 2026-07-29**. The "next dataset": native re-ingest from `data.ecan.govt.nz` of 3 ts_ortho datasets (streamflow/gage_height/precipitation) via the shared `envlib-ingest-base` toolkit + the `envlib-ingest-ecan-env` repo (NOT the tethys migration). Built across **7 dual-blind-reviewed rounds** (Gemini via agy + fresh Claude subagents): pure-numpy plain-data API (`(times, values)` tuples, `datetime64[us]`), frequency-agnostic tsortho (cadence from the `frequency_interval` CV, natural datetime64 units + explicit step, epoch-anchoring guarded both ways), the **two-clocks missing-data pipeline** (local-rolling-median gap rule + `min_coverage` 0.75 coverage rule, incl. sum-slot coverage), QC = the encoding min/max (out-of-range → NaN pre-merge, persisted `valid_min`/`valid_max`), per-key S3 storage + `(1, 25_000)` chunking, and the `station_id`/`station_name`/`station_ref` (+ optional `station_altitude`) `(point,)` nomenclature. **Surfaced + fixed a cfdb encoder BLOCKER** (packed-dtype `encode()` fabricated data for NaN/out-of-range via numpy's undefined float→uint cast → cfdb **0.9.4**). Toolkit shipped **0.1.0 → 0.1.5** on PyPI (0.1.2: chunking default + `station_ref`; 0.1.4: station-var CF metadata + `station_altitude`; 0.1.5: `sentry-sdk` dep); base + ecan images built + pushed (ecan `:0.2`); supervised full backfill done; **swarm cron LIVE — first scheduled run succeeded 2026-07-29**. **Sentry error reporting** wired into the engine (`ingest.py`: opt-in `SENTRY_DSN`, per-dataset `capture_exception` + guarded init; `sentry-sdk` moved into the toolkit — Mike, 2026-07-29 — so every ingest inherits it via the base image). ECan is the proven **copy-the-template** for future ts_ortho extractions (`ingest.py`/`deregister.py` source-agnostic; a new source rewrites only `source.py` + `config.py` + creds + deploy names). Full round-by-round history in git.

- [x] ~~**[ingest] Move the ECan datasets to a dedicated S3 bucket (member `ecan-env-monitoring`; catalogue entry stays in the commons RCG)**~~ — **MIGRATION 2026-07-23 / CLOSEOUT 2026-07-29**. Config/creds refactor split member DATA (per-owner bucket; `[member]` toml base `db_key`/`db_url` + per-dataset `member_key`) from the catalogue ENTRY (commons RCG) — via the source-agnostic `ingest.py`/`deregister.py` + a one-time `migrate.py`. Executed + verified 2026-07-23: gage_height/precipitation copied commons→ecan (ebooklet 0.10.3 `copy_remote`, indirect path — the ecan buckets are cross-account), streamflow FRESH-BACKFILLED into ecan (its commons source was torn — 29 objects never uploaded); all 3 entries repointed IN PLACE (same dvids), fsck-clean, public `Catalogue()` shows `bucket=ecan-env-monitoring`. The orphaned commons copies (gage/precip + the torn streamflow) were **deleted 2026-07-29** via a guarded scoped `delete_remote`. **Data-loss cause: considered resolved (Mike's call — not investigating further)**: the recent stack fixes cover it — booklet 0.12.9's create-path lock-before-truncate race (the prime suspect) + envlib 0.1.3's publish-verify safeguard as the detection layer that now catches a silent over-claim before it is advertised. (The twin `[investigate — DATA LOSS]` item in the stack tracker `envlib/OPEN_WORK.md` closes on the same basis.)

- [x] ~~**[ingest] Move esa-sst off the commons bucket into its own ECMWF member bucket** — first application of the "datasets NEVER live in the commons bucket" policy (Mike, 2026-07-28) to an EXISTING dataset~~ — **DONE 2026-07-28**. esa-sst's ~20 GB grid moved from commons `envlib/datasets/cci-sst-l4-v3-nz` → member `ecmwf-data/envlib/cci-sst-l4-v3-nz`; entry repointed (**same dvid `982d2f9e0767df23b5fa5d3a`**, new public `db_url` `…/file/ecmwf-data/envlib/cci-sst-l4-v3-nz`), commons orphan deleted, dataset still opens credential-free. **Approach: a straight re-push of the local artifact** via `publish.py` (validate → data push → `verify_objects` fsck → atomic entry repoint, ~30 min at ~13 MB/s — the production-proven path; the entry serves the live commons copy until the final atomic repoint, so no absence gap), NOT a server-side copy. **Config refactor** (`envlib-ingest-esa-sst`, staged for Mike's review/commit): `config.py` adopts the `[member]`-toml convention (`member_key` + `MEMBER_TOML=~/.envlib/ecmwf.toml`; base `db_key`/`db_url` from the toml; rstrip'd compose at every site); `publish.py`/`deregister.py`/`verify.py` split commons-creds (catalogue RCG) from member-creds (data); `watch_push.py` retired (superseded by the `ebooklet.push` logger). **Dual-blind reviewed (Fable 5 + Gemini, 2026-07-28), and the review INVERTED the plan**: the same-account server-side `copy_remote` was dropped once both arms found its fast path gates on the identical credential PAIR — not the account (`ebooklet/remote.py:486`) — and that cross-bucket branch has never run live in the stack, while the re-push is proven. Findings folded in: `verify.py`/`watch_push.py` still read the deleted config attrs and would have crashed the acceptance gate (Fable H1); a double-slash `db_url` from the trailing-slash toml base (fixed by rstrip everywhere — M1); member key needs LIST+read+write+delete; the orphan delete is scoped safely by `delete_remote`'s trailing-slash bound (cannot touch the RCG/siblings). Gemini's "re-push silently no-ops into an empty bucket via a stale `.remote_index` sidecar" was **refuted against source** — `create_changelog` (`utils.py:929`) gates on the live `remote_session.uuid` (None for an empty bucket) → the no-remote branch pushes every key regardless of the sidecar. Orphan removed via a guarded one-time script (hard-pinned target + a live-entry-must-have-repointed guard read through a fresh cache + fsck existence check). **Closes the esa-sst companion-fsck-sweep item** (the fresh member fsck is the sweep). Plan: `~/.claude/plans/yes-go-ahead-with-serialized-star.md`. Dry-runs + verify all green; the live re-push/repoint/delete were Mike's supervised runs.

- [x] ~~**[toolkit] Station-variable CF metadata + optional `station_altitude` (toolkit 0.1.4)**~~ — **DONE 2026-07-22; RELEASED as toolkit 0.1.4** (Mike's ask; CODE done + tested). The `station_id`/`station_name`/`station_ref` `(point,)` vars were created with ZERO attributes; now each carries a CF `long_name` (and `station_id`/`station_ref` a `comment`), and **`station_id` carries `cf_role='timeseries_id'`** — the CF discrete-sampling-geometry instance-identifier marker (§9.5; cfdb already writes the global `featureType='timeSeries'`, and its `utils.py:82` `base_attrs` template specified exactly this convention). Attrs **back-fill onto pre-existing datasets** via an idempotent heal in `merge_dataset` (runs *before* the empty-window early return, so even quiet update runs heal; unconditional `.attrs.update` is upload-clean because cfdb's attrs-finalizer writes only on real change — experimentally confirmed bit-identical on re-run). Also adds the optional **`station_altitude`** `(point,)` unpacked-float32 var (CF `standard_name='altitude'`, `units='m'`), created ONLY when a source supplies a non-null `'altitude'` value in the stations dict (a source with none, e.g. ECan — or an all-null column — gets no empty var; NaN per station lacking one), written for new stations on merge with a loud warn-to-rebuild when altitude arrives but no var exists. **Trigger = any non-null altitude; station metadata is write-once** (existing rows not revised on merge, matching name/ref — documented). Built ahead of a concrete altitude source at Mike's direction (overriding the earlier "not speculatively" note — "it won't be long"); `_altitudes` coerces None/absent/NaN→NaN and raises a **ref-named** error on junk / non-finite. **This CLOSES the "[toolkit] altitude as the next well-known station variable" backlog item.** **Independently reviewed TWICE by fresh-context Fable 5 agents** (decorrelated from the Opus author, blind): a design stress-test at plan stage + a blind CODE review of the implementation, both running scratchpad experiments (heal bit-identical/idempotent by sha256, row-alignment under scrambled order, unpacked-float NaN on the 0.9.4 floor, `validate()` + identity-hashing provably indifferent, CF §9.5-conformant). The code review found a real `_altitudes` cluster — junk/non-finite altitude on a NEW-station merge raised *after* the row append (permanent NaN under write-once) + a float32-overflow-to-inf hole (finite float64 → inf, violating the "non-finite raises" contract) + state-dependent loudness — **all fixed**: `_altitudes(sub)` hoisted before any row mutation (pre-append, uniform loudness matching build), finiteness checked against float32 storage (`abs(f) > float32.max`), + a byte-identity churn tripwire test. **Then a design dialogue settled altitude QC + precision (Mike, 2026-07-22):** a **universal plausibility band** `valid_min=-500`/`valid_max=9000` m — physics, above every surface station and below the deepest dry-land depression; the *range* is universal even though the data-var QC bounds are per-source, because station altitude is the same quantity in every dataset — with numeric out-of-band values (±inf, float32 overflow, -9999-type sentinels) becoming **NaN + a named warning** ("the declared range doubles as QC"), and a non-coercible/wrong-type value **raising** (adapter bug). **Precision is deliberately NOT declared** (stored as-reported, unpacked float32): Mike's ruling — a station's survey accuracy varies station-to-station *within* one agency, is rarely documented, and isn't worth investigating, so a declared precision at any granularity would be dishonest (too coarse destroys the high-res surveys; too fine fabricates accuracy for the GPS-only ones); an honest `comment` attr says exactly that instead. Bounds + comment persisted as attrs (re-stamped by the heal). **Verified:** toolkit **35/35** (24 originals + 11 new incl. bad-altitude-raises-pre-mutation, heal-rerun-upload-clean, altitude-out-of-range→NaN+warn) + ruff + black. **Still parked (Mike's call, independent):** a *NaN-only altitude backfill* on merge — a station first appearing before its altitude is populated stays NaN forever under strict write-once; filling a stored-NaN altitude when a later run supplies it would mirror the data merge's non-NaN-incoming-wins philosophy (name/ref stay write-once since they can't be "missing"). Docstring + README station-nomenclature section updated (altitude no longer "future"; write-once + version notes). **Side finding (separate, cfdb): ts_ortho→netCDF export raises `'Point' object has no attribute '_factor'` (`cfdb/main.py:600`) on minimal ts_ortho datasets — pre-existing, independently reproduced here on cfdb 0.9.4, logged to the stack-dev tracker `envlib/OPEN_WORK.md`.** Released as toolkit 0.1.4 on PyPI (Mike); downstream locks refreshed onto it.

- [x] ~~**[envlib+ingest] The esa-sst dataset is LIVE on the public commons — the first real dataset, published twice and lifecycle-proven**~~ — **DONE 2026-07-16**. Mike's original ~20GB push (old 0.10.0 stack, one group/~105s, paused + resumed across days) COMPLETED and registered; its remote state then verified byte-faithful (verify.py steps 1-3 + standalone first-live-fsck: 149 objects, 0 orphans — the pause/resume left zero debris). **Mike then ruled a full lifecycle exercise on the fixed stack**: fidelity gate re-run PASSED (4 gates, artifact certified) → `scripts/deregister.py` retraction (entry removed + pushed first, then data deleted) → **deregistration verified on 4 axes** (public catalogue 0 entries, member prefix 0 objects, public URL 404, catalogue fsck clean) → **republish on booklet 0.12.8/s3func 0.9.5/ebooklet 0.10.1: 149/149 groups, 19,992.5 MB in 25:29 at 13.07 MB/s mean, 0 failures, packs 0.3-0.4s/group (were ~105s), total publish 28.9 min vs ~4.5h originally (~10x), live rate/ETA narration throughout, independent LIST-snapshot corroboration (13.4 MB/s sustained)** → closing verify.py PASSED end-to-end (bare-Catalogue query, antimeridian spatial, byte-equality x3 dates x2 vars via the public URL in 33s cumulative, ancillary demo, fsck both remotes: 149 expected/0 orphans/0 missing). **This also closes the "[ebooklet] First live fsck proof" backlog item** (fsck ran 3x against production: pre-retraction, post-deregister, post-republish — all clean). Round wrinkles fixed in the ingest repo (staged for Mike's commit): verify.py's ice-demo assertion corrected (the >15% Antarctic ice edge never enters this domain — max Sept fraction in-record 0.08; demo now verifies the SST/ice ancillary relationship without asserting phantom ice), publish.py pre-flight PartialDataWarning suppressed-with-justification (the artifact is remote-stamped since its first publish - cfdb's guard working as designed), scripts/deregister.py added (dry-run gated). The commons now serves its first real dataset: `dataset_version_id=982d2f9e0767df23b5fa5d3a`, `https://b2.envlib.xyz/file/envlib/datasets/cci-sst-l4-v3-nz`. *(Data since migrated to the `ecmwf-data` member bucket on 2026-07-28 — this commons URL is retired; see the top Done entry.)*
