# envlib ingestion — Open Work

The **datasets/ingestion track** of the envlib project: this toolkit, the per-source ingest
repos (`envlib-ingest-*`), dataset selection/licensing, and the tethys→envlib data migration.
Stack *development* (envlib/cfdb/ebooklet package work) is tracked separately in
`envlib-repos/envlib/OPEN_WORK.md`. Same conventions: **verification debts** (work done but
not yet proven; remove once the close-check passes) + **backlog**; completed items move to
**Done** at the bottom. Sessions always launch from the envlib hub repo regardless of track.

## Verification debts

*(none currently — the ECan entry below carries its own remaining-items list, which includes
the at-scale backfill as the standing unverified step.)*

## Backlog

- [ ] **[toolkit] `statistic='point'` engine for the resampler** (Mike, 2026-07-20): pass-through of instantaneous samples for native-cadence datasets published as-is — the envlib `aggregation_statistic` CV already has the `'point'` code, and `resample()` currently raises cleanly on it. Motivation: scripts should iterate datasets passing `statistic=metadata['aggregation_statistic']` blindly (mean/sum/point alike) with the resampler owning the dispatch. Design settled in discussion: NO estimation of any kind — after `_prep` (round_to + dedupe), keep only readings exactly on the epoch-anchored freq grid, values untouched; off-grid readings are absent (a point dataset never interpolates to an instant it didn't observe); producers wanting full snapping declare `round_to=freq` explicitly; `max_gap`/`gap_multiplier`/`min_coverage` accepted for API symmetry but unused (no intervals). Note the mean-at-native-freq distinction that motivated this: `statistic='mean'` at source-freq==output-freq correctly yields interval means (endpoint averages, n−1 points) per its declaration — `'point'` is the semantic for identity pass-through. Tiny engine + fixtures + README statistic entry + toolkit version bump; build when the first point-semantics dataset arrives.
- [ ] **[envlib+ingest] ECan surface-water + precip ingest — BUILT + verified, awaiting Mike's review/commit + supervised backfill** (2026-07-16/17; the "next dataset"). Two new repos under `~/git/envlib-repos/ingest/` (both uncommitted, `.venv` present): **`envlib-ingest-base`** — a shared, tested toolkit (`resample.py` exact trapezoidal time-weighted mean + accumulation sum, gap-aware; `tsortho.py` ts_ortho build + idempotent positional merge + publish wrappers) **+ the Docker base image**; **`envlib-ingest-ecan-env`** — thin fetch (`ecan.py`) + `config.py` (3 datasets: streamflow/gage_height/precipitation) + `ingest.py` (swarm-cron entry) + Dockerfile/compose/swarm + PROVENANCE. Native re-ingest from `data.ecan.govt.nz` (NOT tethys migration/Hilltop/DB). **Design was dual-blind reviewed** (plan stage); rulings folded in. **Verified:** 14 toolkit unit tests (golden resample + ts_ortho idempotency) green; full pipeline validated on real data; **live isolated-test-key E2E on B2** — publish→query→credential-less open (byte-equal)→fsck→teardown, and update idempotency (a merge bug — adding all discovered stations not just those with data — was CAUGHT by the live test, fixed, unit-tested); stateless-update confirmed (no swarm volume needed); both Docker images build + import-smoke green; **all 6 test keys swept clean off B2, production catalogue + esa-sst never touched**. Plan-of-record: `~/.claude/plans/i-m-thinking-that-the-foamy-sunset.md`. **`envlib-ingest-base` is packaged for PyPI** (tests moved into the module folder per stack convention + excluded from the wheel; dynamic version, stack ruff config; `uv build` wheel-verified tests-absent; the ecan repo depends on it via a `[tool.uv.sources]` path override, so it resolves from PyPI once published). **2026-07-17 round 2 — pandas REMOVED from `envlib-ingest-base` (pure-numpy toolkit; plan dual-blind reviewed, Gemini via agy + fresh Claude subagent, convergent, no contradictions):** API is now plain `(times, values)` tuples + `{ref: {'lon','lat','name'}}` dicts (the cfdb-shaped form — Mike's ruling: simplest, no wrapper class; a NamedTuple idea was dropped); **time unit is `datetime64[us]`** (Mike's ruling 2026-07-17: ns was pandas legacy — µs is python datetime's native resolution, ±290k-yr range kills the ns wrap-guard complexity outright, matches pandas 3's own new default; note esa-sst's time coord remains ns — mixed units across commons datasets, harmless to readers); **`round_to` parameter added** (Mike, 2026-07-17: declared true timestamp precision per source, rounded before binning — corrects feed jitter + merges double-served readings via the duplicate collapse; ECan config sets `'1min'`; **live-confirmed load-bearing: 66% of sampled ECan precip readings are stamped `:59:59`**); pandas objects still work as *inputs* by duck-typing (pinned by a tz-aware fixture); review catches folded in (out-of-range string dates raise instead of silently wrapping via a `datetime64[us]` route; freq parser raises loudly on anything outside `h/min/s/D`; tuple-emptiness sweep — empty ⇔ `times.size == 0`; `ecan.py:100` + `ingest.py:69` boundary crashes fixed; sum bins documented epoch-anchored, pinned by a `5h` fixture). **Verified:** 39 unit tests green (the 14 goldens preserved exactly + 25 new guardrail fixtures); pandas-absent import + resample proven under an exact `uv sync --no-dev` (no stack package pulls pandas); ruff + black clean; live one-site smoke green (discover 133 stations → fetch → resample → build_local → validate, scratchpad only, commons untouched); ecan repo boundary adapted (keeps pandas internally for CSV; `import pandas` gone from `ingest.py`). **2026-07-17 round 3 — tsortho GENERALIZED to frequency-agnostic** (round 2 committed by Mike as `b65c03d` first; this round's plan dual-blind reviewed — Gemini via agy + fresh Claude subagent, convergent except the phase question, resolved as strict-epoch-anchoring guarded BOTH directions): cadence now derives from `meta.frequency_interval` (envlib's closed CV is the single source of truth — no freq param; `month`/`year`/`None` raise); the time coord is stored in the cadence's **natural datetime64 unit** (`day`→`[D]`, hourly family→`[h]`, minute family→`[m]`) with an **explicit step** (closes cfdb's step=None holes + a numpy deprecation); alignment guard (incoming timestamps must sit on the epoch step grid) + metadata phase guard (fixed cadence with reduced `utc_offset`≠`+00:00` rejected — identity-hashed metadata can never lie about phase); merge cross-checks the stored axis (origin residue + full-diff density) and refuses windows predating the axis (pre-existing negative-slice bug fixed); `hourly`→`series` rename, report key `new_hours`→`new_steps`; resampler accepts the CV code `'day'`; ecan passes `freq=cfg['metadata']['frequency_interval']` so fetch/storage can't drift. **Verified:** 55 tests green (goldens intact + the freq matrix: daily/15min/12h builds+merges, single-timestep, legacy-`[us]`-axis merge, pre-1970 daily, both guards, doctored-attr cross-check, revise-only, window-before-axis), ruff+black clean, `Catalogue.validate()` passes for `[D]`/`[m]`/`[h]` coords in-suite. **2026-07-17/18 round 4 — Phase-7 dual-blind CODE review DONE + all findings fixed** (Gemini via agy + fresh Claude subagent, blind, 19 stated invariants attacked; Claude ran a 4,000-case property test of the trapezoid mean against an independent reference — 0 mismatches; repos audited clean). **Headline: a cfdb BLOCKER — packed-dtype `encode()` fabricated data for unencodable values** (NaN → 214747.36 on the streamflow uint32 encoding via numpy's undefined float→uint SIMD cast; finite out-of-range wrapped into plausible readings; small arrays took the scalar path and round-tripped "correctly", which is how every suite missed it) — **FIXED in cfdb 0.9.4 (in-tree, unreleased)**: encode maps non-finite/NaT/out-of-range → fillvalue; 11 new dtypes tests; cfdb 229/229 incl. live; fix proven E2E through tsortho on the overlay; **live esa-sst verified UNAFFECTED** (uint16 cast NaN→0=fillvalue at write time; bytes on B2 permanently correct); toolkit also carries an exact `_nan_safe` shield (NaN→offset ≡ fillvalue) so it is safe on any installed cfdb. **Rulings (Mike, 2026-07-17/18), all implemented + documented in the toolkit README ("Missing data and aggregation rules")**: (a) **min/max = QC bounds** — encoding range doubles as plausibility filter, out-of-range → NaN at the toolkit boundary BEFORE the merge combine (a rejected value never displaces stored valid data; `qc_rejected` in the merge report; ancillary QC flag deferred); (b) **zombie series refs raise** in build+merge (station-list-driven extraction: no metadata → no row; build's silent-drop bug fixed); (c) **the two-clocks missing-data pipeline** — gap rule on the data's clock (`gap_multiplier ×` **LOCAL rolling-median** native interval — fixes the within-station regime-change failure of a global median AND drops the accidental freq floor that bridged 30-min outages on 5-min sites), coverage rule on the output's clock (**`min_coverage` 0.75 default**, per climatological completeness practice + adversarial missingness, now applied to the **sum too** via accumulation-slot coverage — an hour with 1 of 12 rain slots is missing, not an undercount; per-slot-total assumption documented). Also fixed: freq grammar rejects `'0min'`/zero/negative/NaT/calendar durations; `_remote_exists` no longer swallows transient errors into a full-rebuild path; **gap healing** — `gap_steps` in the merge report + ingest auto-refetches `1_Week` to heal ≤1-week holes (NaN holes are refillable), deeper holes raise for a supervised backfill; `time_chunk` config wired into build; docstring/README corrections (trapezoid-vs-plain-mean, µs-range claim). **Verified:** toolkit 70/70 + ruff + black; cfdb 229/229; ecan imports; live one-site smoke on final semantics (fetch → resample at 0.75 → build → validate). **2026-07-18 round 5 — the fix round itself dual-blind reviewed (Mike's call: unplanned changes warrant it) + all findings fixed.** The reviewers DISAGREED twice and each landed a solo blocker — synthesis: (a) **Claude blocker, author-verified: the `_nan_safe` shield could NOT make cfdb 0.9.3 safe** — cfdb's own partial-chunk read-modify-write re-encodes STORED holes on every merge (reproduced: an ordinary 6-h merge fabricated 214747.36 into all 10 cells of a stored outage hole at production chunk width), and fabricated cells are non-NaN so no heal can ever fix them → fixed structurally: **cfdb pin `>=0.9.4` (HARD floor) + `[tool.uv.sources]` local cfdb overrides in both ingest repos (DROP at release; CI red until then) + runtime version floor in tsortho + a SIMD-width hole-survives-merge tripwire test** (this bug class evaded suites twice via the tiny-array scalar-cast path); shield kept as belt-and-braces with an honest docstring; **release ordering is now load-bearing: cfdb 0.9.4 MUST be on PyPI + in locks BEFORE the backfill/cron**; (b) **edge-median disagreement** (Gemini: short leading era erased by nearest-full-window edge rule; but its edge-padding fix is what the Claude round proved fails for trailing outages) → resolved with **centered shrinking windows** at the edges, surviving both counterexamples, fixture-pinned both ways; (c) **sum-span disagreement** (Gemini verified the formula matched the README; Claude proved the SPEC credited skipped slots — an hour missing every 3rd rain slot published as complete) → **spans capped at one local native slot** (`min(d, med)`), README/docstrings corrected; (d) **singleton sums** (Gemini blocker: 1 reading published as fully covered while 2 readings were rejected) → singletons contribute zero coverage (kept only at `min_coverage=0`); (e) **QC now against the DECLARED min/max** (was the wider encodable range — negative rainfall passed), persisted as `valid_min`/`valid_max` var attrs, encodable range as legacy fallback; **±inf now rejected + counted**; (f) cfdb guard: float-branch compares in float64 (float32/uint32 top-edge band), packed-Integer beyond-width now RAISES (ints have no missing value — Gemini's map-to-fillvalue would decode as real data); (g) straddling merge windows report `dropped_before_axis` + warn; healing converts `gap_steps`→hours via the CV before the 1-week bound; build chunk point-dim = full discovery count. **Verified:** toolkit 76/76 + ruff + black on **locked local cfdb 0.9.4**; cfdb 231/231 incl. live; ecan imports; live one-site smoke (45 h, `valid_min`/`valid_max` persisted, validate clean). **cfdb 0.9.4 RELEASED to PyPI (Mike, 2026-07-18** — commit `55cce15`; wheel+sdist verified on PyPI; encode guard finalized with native-dtype bounds after Mike's operational-write-path performance challenge: guard overhead ~0.1 ms per 500k-value chunk, clean-chunk `np.where` skipped, semantics bit-identical to the reviewed version, cfdb 231/231**)**. Post-release sweep done: both `[tool.uv.sources]` cfdb overrides DROPPED, toolkit+ecan relocked onto PyPI 0.9.4 (toolkit 76/76 incl. the SIMD tripwire + ruff + black on the released package; ecan imports green), **envlib lock upgraded to cfdb 0.9.4 + full suite 144/144** (the standing downstream gate). CI is un-red again (the pin now resolves). Changelog nit: the released 0.9.4 changelog entry still reads "(unreleased)" — fix the date next time the repo is touched. **2026-07-18 round 6 — resampler API collapsed to a single `resample()` (Mike's call, pre-publish):** `resample_mean`/`resample_sum`/`resample_station` replaced by one `resample(times, values, *, statistic='mean', ...)` — `statistic` keyed to envlib's `aggregation_statistic` CV exactly as tsortho keys cadence to `frequency_interval`; the two signal models (instantaneous vs accumulation) stay separate internal engines (`_resample_mean`/`_resample_sum`, dual-reviewed numerics untouched — pure entry-point plumbing), future median/min/max of the instantaneous signal slot in without API change, unknown statistics raise listing supported. Killed the ecan config's `kind` duplication (`cfg['kind']` vs `metadata.aggregation_statistic` could drift — now the metadata drives both computation and identity). 77/77 + ruff + black; README/examples reworked + verified; ecan imports green. **envlib-ingest-base 0.1.0 RELEASED to PyPI (Mike, 2026-07-19** — commit `bf7af81`; wheel+sdist verified; requires_dist exactly right: NO pandas, `cfdb>=0.9.4`)**. Post-release sweep done (2026-07-19): clean-room fresh-install-from-PyPI proof (imports, pandas absent from the closure, tests excluded from the wheel, both statistics compute correctly — first attempt was contaminated by a stale scratchpad `.venv` uv discovered by walking up from cwd, removed; the released closure is clean); ecan's `[tool.uv.sources]` toolkit override DROPPED + relocked onto the PyPI wheel (needed `--reinstall-package` — same-version editable survives a plain sync) + live one-site smoke on the fully-released stack (rain/sum path, statistic keyed from metadata, build+validate clean). The ecan repo now consumes ONLY released packages. **2026-07-19 round 7 — Mike's review rulings, implemented:** (a) **daily deep sweep** (the tethys pattern): hourly runs fetch `2_Days`, but once a day (`DEEP_UPDATE_UTC_HOUR=12` ≈ midnight NZST) the run fetches `1_Week` — the only mechanism that collects station-level late-arriving backlog (a station offline for days leaves NO axis hole, so `gap_steps` healing is blind to it; NaN-only overwrite makes the wide re-merge idempotent); the blast-radius guard now scales with the run's own window (`PERIOD_HOURS[period] + 24h` margin; the old fixed 72h would have rejected the weekly sweep), and the healing bound derives from `DEEP_UPDATE_PERIOD` (any hole behind a deep run ⇒ downtime >1wk ⇒ supervised — emergent, no special case); (b) **per-key S3 storage** (`num_groups=None`) for the ECan datasets — Mike's ruling, grounded in ebooklet source: per-key reads/pushes move exactly the changed chunk (grouped storage byte-range-coalesces reads and rewrites WHOLE group objects per push — pure ballast at ~40 keys/dataset and hourly updates); **the rule, now in the toolkit README: per-key for continuously-updated ts_ortho, grouped only for large rarely-updated archives (thousands of keys, ebooklet's 10–100MB/group guidance)**; choice is fixed at first publish — settled before the backfill, which was the point of the review; `num_groups` config keys deleted, `time_chunk` values kept (already in the 2–3MB pre-compression zone; the chunk is now the S3 object + atomic update unit); (c) confirmed in-place merge semantics for Mike (no temp files; dead space in the single booklet file is deliberate, prunable, and moot under the ephemeral-cache swarm pattern). (d) **streamflow `max_value` 100,000 → 20,000 m³/s** (Mike's review question; researched: ECan feed's own full-history maxima Waimakariri 2,761 / Waitaki 1,738 — the dammed Waitaki peaks BELOW the unregulated Waimakariri despite the largest mean, and no Rakaia flow site exists in the ECan discovery; literature: Canterbury record = Rakaia 5,594 m³/s (1994), NZ all-time = Buller ~12,700 (1926)); the bound is a deliberately blunt NZ-wide plausibility filter — above anything ever recorded so no real flood is rejected, applied to instantaneous raw readings at fetch (hourly means only lower); per-station range checks are a future QC'd product's job, not this raw feed's; still uint32, 4× better top-of-range float32 honesty; another baked-at-build parameter settled BEFORE the backfill. **Compat note:** ecan passes `num_groups=None` explicitly (the released toolkit 0.1.0 requires the kwarg — bind-checked against the PyPI wheel); the toolkit tree carries **0.1.1-unreleased** (num_groups now optional/default-None + the storage-layout guidance docs) for the next publish. Verified: toolkit 77/77+lint, ecan imports + call-shape binds, deep-sweep/guard/healing logic clock-mocked at both cron hours. **2026-07-19: toolkit 0.1.1 RELEASED (Mike, commit `266f36a`; PyPI-verified) + Docker layer sweep:** Mike bumped ecan's pin to `>=0.1.1` + removed pandas from `requirements_base.txt` (base image now pandas-free like the toolkit) → knock-ons closed: **ecan Dockerfile now installs its own pandas** (base installs toolkit `--no-deps`, so the base's pandas removal would have crashed `import pandas` at container start); **`requirements_envlib.txt` cfdb pin bumped `>=0.9.3`→`>=0.9.4`** (with `--no-deps` toolkit install, that file is the ONLY encoder-fix floor enforcement in the image); ecan relocked onto PyPI 0.1.1 (verified from site-packages, `num_groups` optional confirmed) and the three now-obsolete explicit `num_groups=None` kwargs removed. Also: streamflow QC bound + stage-bound rationale review (see round 7d). **2026-07-19: Docker requirements files ELIMINATED (Mike's call — declared-twice deps, per the metadata-single-source-of-truth principle; the stale `cfdb>=0.9.3` drift we'd just caught was the live proof):** the base Dockerfile now installs the **released toolkit from PyPI** (`TOOLKIT_VERSION` build arg = image tag; deps flow from the package's own `requires_dist`, incl. the cfdb 0.9.4 floor); `requirements_base.txt` + `requirements_envlib.txt` deleted; ecan `FROM` bumped to `:0.1.1`. **Both images REBUILT + smoke-tested** (base: toolkit 0.1.1/cfdb 0.9.4/envlib 0.1.2, pandas-free as designed; ecan: pandas via its own layer, scripts import, statistics keyed from metadata) — the stale-images backlog item is substantively closed; what remains Docker-wise is Mike's namespace confirmation + pushing `mullenkamp/envlib-ingest-base:0.1.1` and (post-review) the ecan `:0.1` images to the registry. **2026-07-20 closeout:** repos committed (Mike); `envlib-ingest-base:0.1.1` + the ecan image **built and PUSHED to Docker Hub** (Mike); **PROVENANCE.md licence TODO closed** — the verbatim CC-BY-4.0 notice captured from the data portal's own Agreement page (URL + capture date + the side-conditions: attribution-without-branding, no advertising, currency responsibility) with a compliance mapping; notably the CC statement lives on the DATA PORTAL agreement page only — the general ECan website copyright page has no CC mention. **2026-07-20: ts_ortho CHUNKING ruling (Mike, mid-backfill review)** — point dim = **1** (was all-stations: the dominant consumer read is one station's history, and an all-stations point chunk forces a whole-dataset download to read one station, ~250× amplification; point=1 also keeps per-station updates/appends independent), time_chunk **25,000** (was 6000/8000 — with point=1 that meant ~18k S3 objects across the datasets; 25k ⇒ ~6 objects/station, ~50–100 KB chunks, a few MB tail-chunk upload per update run). Toolkit auto-default updated to `(1, 25_000)` with the rationale in-code (**0.1.2-unreleased** — future release, not needed for the re-backfill since ecan passes chunk_shape explicitly); ecan config/ingest updated; envlib skill records the priorities. Also added per-site progress logging to `fetch_and_resample` (variable header + `[i/n] ref: readings→intervals`), captured the PROVENANCE verbatim licence, compose-v1 `version:` fix. **The backfill that STARTED on the old all-stations chunking must be REPLACED**: chunking is baked at build, so the re-run needs (a) the partially/fully-written remote datasets cleared off the commons first — the ingest's `_remote_exists` check would otherwise route the re-run into the UPDATE path against the old layout — and (b) the ecan image rebuilt + pushed (it carries the new config/ingest/logging; the toolkit change is default-only and not on the critical path). **Remaining:** (1) stop/discard the running backfill + (2) clear the remote — **DONE 2026-07-20 (Mike)**: backfill stopped; streamflow (the only dataset that had pushed + registered) retracted via `deregister.py` (`delete_data=True`); verified clean — a fresh-cache public `Catalogue()` lists only esa-sst, all three member URLs 404, and an S3 prefix list shows **0 objects under `datasets/ecan*`** (the re-run will correctly take the BACKFILL path). The retraction surfaced two stack defects, recorded in `envlib/OPEN_WORK.md`: [ebooklet] remote key deletions never propagate to warm local caches (a warm `.rcg` keeps serving the retracted entry — delete the cached pair as the workaround), and [booklet] opening a locked file blocks forever silently (incl. same-process — the "hanging open()" was a self-deadlock against an unclosed earlier handle). (3) rebuild + push the ecan image; (4) re-run the **supervised full backfill** on the new chunking; (5) enable the swarm cron. Then this whole entry moves to Done.
- [ ] **[ingest] Phase-anchored datasets (local-midnight / 9am rain-day boundaries)** (designed follow-up from the 2026-07-17 tsortho-generalization review; Gemini argued phase-now, Claude argued the guard-first path — adopted): today tsortho enforces strict epoch anchoring in BOTH directions (data alignment guard + metadata phase guard: fixed cadence with reduced `utc_offset`≠`+00:00` rejected). Supporting phase needs ONE designed feature: resampler `origin` parameter (bin boundaries at epoch+phase) + phase-aware tsortho guard/axis (phase from the hashed `utc_offset`, NOT the unhashed axis t0) + natural-unit rule becomes `gcd(step, phase)` (a `+12:00` daily axis needs `[h]`, not `[D]`) — and note the 9am NZ rain day may not be expressible via `utc_offset` at all (±14h/quarter-hour clamp), so the origin feature is the primary path. Pick up before any local-day daily product (e.g. the parked NZ precip candidate).
- [ ] **[tethys] Build the tethys → envlib migration pipeline** (added 2026-07-08; the next major project phase after public-RCG hosting): walk the tethys production datasets into envlib form — apply the plan's migration notes (feature/variable renames, frequency + utc_offset conversion, product_code decomposition, interval-start timestamps), re-derive ids via `Metadata`, build cfdb files, publish into the commons catalogue. Scope/tooling (where the pipeline lives, per-repo vs central) to be planned when picked up; `docs/guide/tethys-migration.md` + the plan's migration-notes section are the specs.
- [ ] **[envlib] First commons datasets — static-first, fresh-first (Mike's direction, 2026-07-14; dataset choice OPEN)**: fill the commons starting with STATIC datasets (not continuously updated — no update pipeline needed, defers the quasi-static/back-fill machinery), and preferably first a dataset NOT already in tethys — so the first real publish exercises envlib's native producer flow (metadata authoring from scratch, CV coverage, license/attribution) rather than the tethys migration adapter, and the first commons entry is new value rather than duplicated data. Candidate OPEN again (2026-07-14: Mike parked the precip product as the FIRST dataset — the NIWA licensing complication makes it a poor opener; 'I don't want a complicated first dataset'; he is choosing another). One-question filter for candidates, learned from the precip round: can the license/provenance file be produced TODAY? PARKED LATER CANDIDATE — the `~/git/monitoring-data-processing` NZ precipitation product (ts_ortho; hourly + daily; climate-combined provenance level; ~4.2k stations, records to 1852; frozen June-2026 snapshot). Ownership ruling direction: owner = the curator (GHCN model — the merge/QC/combine are editorial acts), sources honored via CC-BY attribution. **Licensing gate**: the NIWA-sourced portion (~46% of station-rows) has licence restrictions incompatible with open republication (analysis + resolution paths in project memory: `project_niwa_licensing`); the publishable path is the CC-BY-only variant (~3.0k of 5.6k station-rows, councils+FENZ+MetService; requires a pipeline re-run with NIWA sources excluded). Ingest-template requirement adopted from this round: a per-source PROVENANCE RECORD in every ingest repo (license text or written confirmation + acquisition date + channel); for councils, the CC-BY notice usually lives on their data-access pages — capture it at acquisition time. The tethys migration follows once the native flow has shaken out.

## Done (newest first)

- [x] ~~**[envlib+ingest] The esa-sst dataset is LIVE on the public commons — the first real dataset, published twice and lifecycle-proven**~~ — **DONE 2026-07-16**. Mike's original ~20GB push (old 0.10.0 stack, one group/~105s, paused + resumed across days) COMPLETED and registered; its remote state then verified byte-faithful (verify.py steps 1-3 + standalone first-live-fsck: 149 objects, 0 orphans — the pause/resume left zero debris). **Mike then ruled a full lifecycle exercise on the fixed stack**: fidelity gate re-run PASSED (4 gates, artifact certified) → `scripts/deregister.py` retraction (entry removed + pushed first, then data deleted) → **deregistration verified on 4 axes** (public catalogue 0 entries, member prefix 0 objects, public URL 404, catalogue fsck clean) → **republish on booklet 0.12.8/s3func 0.9.5/ebooklet 0.10.1: 149/149 groups, 19,992.5 MB in 25:29 at 13.07 MB/s mean, 0 failures, packs 0.3-0.4s/group (were ~105s), total publish 28.9 min vs ~4.5h originally (~10x), live rate/ETA narration throughout, independent LIST-snapshot corroboration (13.4 MB/s sustained)** → closing verify.py PASSED end-to-end (bare-Catalogue query, antimeridian spatial, byte-equality x3 dates x2 vars via the public URL in 33s cumulative, ancillary demo, fsck both remotes: 149 expected/0 orphans/0 missing). **This also closes the "[ebooklet] First live fsck proof" backlog item** (fsck ran 3x against production: pre-retraction, post-deregister, post-republish — all clean). Round wrinkles fixed in the ingest repo (staged for Mike's commit): verify.py's ice-demo assertion corrected (the >15% Antarctic ice edge never enters this domain — max Sept fraction in-record 0.08; demo now verifies the SST/ice ancillary relationship without asserting phantom ice), publish.py pre-flight PartialDataWarning suppressed-with-justification (the artifact is remote-stamped since its first publish - cfdb's guard working as designed), scripts/deregister.py added (dry-run gated). The commons now serves its first real dataset: `dataset_version_id=982d2f9e0767df23b5fa5d3a`, `https://b2.envlib.xyz/file/envlib/datasets/cci-sst-l4-v3-nz`.
